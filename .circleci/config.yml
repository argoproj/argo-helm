version: 2.1
jobs:
  lint:
    docker:
      - image: quay.io/prometheus/busybox
    steps:
      - run: "true"
  publish:
    docker:
      - image: quay.io/prometheus/busybox
    steps:
      - run: "true"
workflows:
  version: 2
  workflow:
    jobs:
      - lint
      - publish
